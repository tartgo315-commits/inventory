# 本地 wrangler pages dev：复制为 wrangler.toml 并填写 database_id。
# 生产部署使用仓库根 _worker.js（Pages Advanced Mode），D1 以 Dashboard Bindings（变量名 DB）为准。
name = "inventory"
pages_build_output_dir = "."
compatibility_date = "2025-04-01"

[[d1_databases]]
binding = "DB"
database_name = "tartgo-inventory"
database_id = "249b0605-3ed1-42cb-b0b0-aec821ea59fa"
